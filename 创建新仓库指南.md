# 创建新GitHub仓库并迁移Maven项目

## 第一步：在GitHub网页上创建新仓库（你来操作）

### 1. 登录GitHub并创建仓库

1. **访问GitHub**: https://github.com/new

2. **填写仓库信息**：
   ```
   Repository name: maven-tomcat-webapp
   Description: Maven + Tomcat Web应用 - 基于Java 17的企业级Web项目
   Public/Private: 选择 Public（公开）

   ❌ 不要勾选 "Add a README file"
   ❌ 不要勾选 "Add .gitignore"
   ❌ 不要选择 "Choose a license"

   保持完全空白！
   ```

3. **点击绿色按钮**: "Create repository"

4. **记下仓库地址**（创建后会显示）：
   ```
   https://github.com/wangsi001/maven-tomcat-webapp.git
   ```

---

## 第二步：告诉我仓库地址

创建完成后，**把仓库地址告诉我**，格式像这样：
```
https://github.com/wangsi001/maven-tomcat-webapp.git
```

然后我会自动帮你：
- ✅ 准备Maven项目代码
- ✅ 初始化新的Git仓库
- ✅ 推送代码到新仓库
- ✅ 设置master为默认分支

---

## 推荐的仓库名称

建议使用以下名称之一：
- `maven-tomcat-webapp` （简洁）
- `java-web-application` （通用）
- `servlet-jsp-demo` （描述性）
- `tomcat-web-project` （清晰）

---

## 创建好后你会看到

GitHub会显示类似这样的页面：

```bash
Quick setup — if you've done this kind of thing before

HTTPS: https://github.com/wangsi001/maven-tomcat-webapp.git

…or create a new repository on the command line
echo "# maven-tomcat-webapp" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/wangsi001/maven-tomcat-webapp.git
git push -u origin main

…or push an existing repository from the command line
git remote add origin https://github.com/wangsi001/maven-tomcat-webapp.git
git branch -M main
git push -u origin main
```

**只需要把那个HTTPS链接告诉我就行！**

---

## 常见问题

### Q: 必须是Public吗？
A: 不一定，可以选Private，但Public方便分享和展示。

### Q: 名字必须是maven-tomcat-webapp吗？
A: 不是，你可以用任何喜欢的名字，告诉我就行。

### Q: 需要勾选那些选项吗？
A: **不需要！保持空仓库，什么都不要勾选！**

---

## 创建完成后

**把仓库地址发给我，我立即帮你推送代码！** 🚀

格式：
```
https://github.com/你的用户名/仓库名.git
```
